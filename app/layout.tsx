import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Franklin's Earthmoving Inc. | New Mexico's Trusted Earthmoving Leader Since 1958",
  description:
    "Franklin's Earthmoving Inc. has served New Mexico since 1958. Specializing in Earthmoving, Utilities, Concrete, and Paving for Commercial, Industrial, Government, and Residential clients.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
